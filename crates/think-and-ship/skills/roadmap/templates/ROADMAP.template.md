# <Project> Roadmap

<!-- Edited by both humans and the /roadmap skill. The skill mutates this on
     each iteration: moves the just-finished chunk to Done, appends discoveries
     to Backlog. Re-prioritization is a manual edit — the skill won't reorder
     Pending without your say-so. -->

## Verify

<!-- One-time setup: how does this project verify a chunk is done? The skill
     auto-detects common toolchains; record any project-specific extras here. -->

- Tests: `<command>`
- Lint:  `<command>`
- Types: `<command>`
- Other: `<command>`

## In progress

<!-- 0 or 1 chunk at a time. Marked [~]. The skill will close this out before
     starting the next pending chunk. -->

## Pending (priority order)

- [ ] **Phase 1 — Title** — one-line description
  - Files: src/foo.rs, tests/foo.rs
  - Acceptance: <concrete, testable definition of done>
  - Notes: <links, prior art, gotchas>

- [ ] **Phase 2 — Title** — description
  - ...

## Done

<!-- Append-only history. One line per chunk + commit hash if committed. -->

## Discovered / backlog

<!-- Unprioritized items surfaced during prior iterations. Promote to Pending
     manually when you want them worked. -->
